# OpenCode Web Docker 环境变量示例 | OpenCode Web Docker Environment Variables Example
# 复制此文件为 .env 并根据实际情况修改 | Copy this file to .env and modify according to your setup

## 通用配置 | General Configuration
# Docker 镜像名称 | Docker image name
# 默认为 GitHub Container Registry 的镜像 | Defaults to GitHub Container Registry image
DOCKER_IMAGE=ghcr.io/swaybien/opencode-web-docker:latest

# 主机端口映射 | Host port mapping
# 容器内端口由 OPENCODE_SERVER_PORT 定义 | Container port is defined by OPENCODE_SERVER_PORT
OPENCODE_SERVER_HOST_PORT=4096

## OpenCode API 密钥 | OpenCode API Keys
# OpenCode 官方 API 密钥 | OpenCode official API key
# 必填 | Required
OPENCODE_API_KEY=your_opencode_api_key_here

## 模型提供商 API 密钥 | Model Provider API Keys
# 根据您使用的模型提供商配置相应的密钥 | Configure keys for model providers you use

# Anthropic Claude API 密钥 | Anthropic Claude API key
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API 密钥 | OpenAI API key
OPENAI_API_KEY=your_openai_api_key_here

# DeepSeek API 密钥 | DeepSeek API key
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 注意：您至少需要配置一个模型提供商的 API 密钥 | Note: You need at least one model provider API key configured

## OpenCode Web 服务器配置 | OpenCode Web Server Configuration

# 服务器端口 | Server port
# 容器内监听端口，默认为 4096 | Container listening port, defaults to 4096
OPENCODE_SERVER_PORT=4096

# 服务器主机名 | Server hostname
# 绑定地址，默认为 0.0.0.0（所有接口） | Bind address, defaults to 0.0.0.0 (all interfaces)
OPENCODE_SERVER_HOSTNAME=0.0.0.0

# 基本身份验证用户名 | Basic authentication username
# 如果设置，启用 HTTP 基本身份验证 | If set, enables HTTP basic authentication
OPENCODE_SERVER_USERNAME=admin

# 基本身份验证密码 | Basic authentication password
# 如果设置，启用 HTTP 基本身份验证 | If set, enables HTTP basic authentication
OPENCODE_SERVER_PASSWORD=your_secure_password_here

# 启用 mDNS（多播 DNS）服务发现 | Enable mDNS (multicast DNS) service discovery
# 在本地网络中自动发现服务器 | Automatically discover server in local network
OPENCODE_SERVER_MDNS=false

# mDNS 域名 | mDNS domain name
# 自定义 mDNS 域名，默认为空 | Custom mDNS domain name, defaults to empty
OPENCODE_SERVER_MDNS_DOMAIN=

# CORS 允许的来源 | CORS allowed origins
# 逗号分隔的域名列表，例如：http://localhost:3000,https://example.com | Comma-separated list of domains, e.g.: http://localhost:3000,https://example.com
OPENCODE_SERVER_CORS=

## 高级配置 | Advanced Configuration

# 禁用 chsrc 镜像源配置 | Disable chsrc mirror configuration
# 如果设置为 true 或 1，跳过 chsrc 设置（适用于国内网络环境） | If set to true or 1, skip chsrc setup (useful for network environment in China)
DISABLE_CHSRC=false

## 可选配置 | Optional Configuration
# GitHub 密钥 | GitHub token
# GITHUB_TOKEN=ghp_xxx

# 以下变量在 docker-compose.yml 中未使用，但可能对调试有用 | The following variables are not used in docker-compose.yml but may be useful for debugging

# OpenCode 调试模式 | OpenCode debug mode
# OPENCODE_DEBUG=false

# OpenCode 日志级别 | OpenCode log level
# OPENCODE_LOG_LEVEL=info

# OpenCode 数据目录 | OpenCode data directory
# OPENCODE_DATA_DIR=/home/node/.local/share/opencode
